可使用下面这个属性同时设置四个角的圆角
```css
border-radius: 10px;
```

还可以使用`50px / 15px;`表示垂直和水平  效果:椭圆角 
还可以使用以下四个属性单独设置每个角的圆角：
```css
border-top-left-radius
```

```css
border-top-right-radius
```

```css
border-bottom-right-radius
```

```css
border-bottom-left-radius
```
