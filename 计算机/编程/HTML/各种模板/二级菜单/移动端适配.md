
```html
<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body { font-family: sans-serif; background: #f4f4f4; }
        
        .mobile-nav { width: 100%; background: #fff; border-top: 1px solid #ddd; }
        .nav-group { border-bottom: 1px solid #eee; }

        /* 一级菜单按钮 */
        .nav-btn {
            width: 100%;
            padding: 15px;
            text-align: left;
            background: #fff;
            border: none;
            font-size: 16px;
            color: #333;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
        }

        /* 二级菜单 */
        .sub-nav {
            display: none; /* 默认隐藏 */
            background: #f9f9f9;
        }
        
        .sub-nav a {
            display: block;
            padding: 12px 15px;
            text-decoration: none;
            color: #666;
            border-bottom: 1px solid #eee;
        }

        /* 简单的展开动画 */
        .sub-nav.open { display: block; }
        
        /* 箭头旋转效果 (可选) */
        .arrow { transition: transform 0.2s; }
        .nav-btn.active .arrow { transform: rotate(180deg); }
    </style>
</head>
<body>

    <nav class="mobile-nav">
        <div class="nav-group">
            <button class="nav-btn" onclick="toggleSubmenu(this)">
                <span>我的服务</span>
                <span class="arrow">▼</span>
            </button>
            <div class="sub-nav">
                <a href="#">订单查询</a>
                <a href="#">售后服务</a>
                <a href="#">收藏夹</a>
            </div>
        </div>
        
        <div class="nav-group">
            <button class="nav-btn" onclick="toggleSubmenu(this)">
                <span>分类浏览</span>
                <span class="arrow">▼</span>
            </button>
            <div class="sub-nav">
                <a href="#">数码产品</a>
                <a href="#">服装鞋帽</a>
            </div>
        </div>
    </nav>

    <script>
        function toggleSubmenu(btn) {
            // 找到按钮旁边的 sub-nav 元素
            const submenu = btn.nextElementSibling;
            
            // 切换显示状态
            submenu.classList.toggle('open');
            
            // 切换按钮状态（用于旋转箭头）
            btn.classList.toggle('active');
        }
    </script>

</body>
</html>
```