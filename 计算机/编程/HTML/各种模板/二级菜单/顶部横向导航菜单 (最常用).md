

```html
<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <style>
        /* 基础重置 */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: sans-serif; }

        /* 导航条容器 */
        .navbar {
            background: #333; /* 背景色 */
            display: flex;
            list-style: none;
        }

        /* 一级菜单项 */
        .nav-item {
            position: relative; /* 关键：为了让子菜单相对它定位 */
        }

        /* 链接样式 */
        .nav-item > a {
            display: block;
            padding: 15px 20px;
            color: #fff;
            text-decoration: none;
            background: #333;
        }
        
        .nav-item > a:hover { background: #444; }

        /* 二级菜单 (默认隐藏) */
        .submenu {
            display: none;
            position: absolute;
            top: 100%; /* 在父元素下方 */
            left: 0;
            background: #fff;
            min-width: 150px;
            list-style: none;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            z-index: 999;
        }

        /* 二级菜单项 */
        .submenu li a {
            display: block;
            padding: 10px 15px;
            color: #333;
            text-decoration: none;
            border-bottom: 1px solid #eee;
        }

        .submenu li a:hover { background: #f0f0f0; }

        /* 核心逻辑：鼠标悬停时显示二级菜单 */
        .nav-item:hover .submenu {
            display: block;
        }
    </style>
</head>
<body>

    <ul class="navbar">
        <li class="nav-item"><a href="#">首页</a></li>
        
        <!-- 包含二级菜单的项 -->
        <li class="nav-item">
            <a href="#">产品中心 ▼</a>
            <ul class="submenu">
                <li><a href="#">软件开发</a></li>
                <li><a href="#">硬件销售</a></li>
                <li><a href="#">技术咨询</a></li>
            </ul>
        </li>

        <li class="nav-item"><a href="#">关于我们</a></li>
    </ul>

</body>
</html>

```

