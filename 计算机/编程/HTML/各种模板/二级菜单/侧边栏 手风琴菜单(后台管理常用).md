

```html
<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: sans-serif; padding: 20px; }
        
        .sidebar { width: 250px; background: #2c3e50; color: #fff; }
        .menu-list { list-style: none; }
        
        /* 菜单项 */
        .menu-item { border-bottom: 1px solid #34495e; }
        
        /* 触发按钮 */
        .menu-title {
            padding: 15px;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            background: #2c3e50;
            transition: 0.3s;
        }
        .menu-title:hover { background: #1a252f; }

        /* 二级菜单容器 */
        .submenu {
            display: none; /* 默认隐藏 */
            background: #1a252f;
        }
        
        .submenu a {
            display: block;
            padding: 10px 15px 10px 30px; /* 左侧缩进 */
            color: #bdc3c7;
            text-decoration: none;
        }
        .submenu a:hover { color: #fff; }

        /* 激活状态 (通过JS添加) */
        .menu-item.active .submenu { display: block; }
        .menu-item.active .menu-title { background: #1a252f; border-left: 4px solid #3498db; }
    </style>
</head>
<body>

    <div class="sidebar">
        <ul class="menu-list">
            <li class="menu-item">
                <div class="menu-title" onclick="toggleMenu(this)">系统管理</div>
                <div class="submenu">
                    <a href="#">用户列表</a>
                    <a href="#">角色权限</a>
                </div>
            </li>
            <li class="menu-item">
                <div class="menu-title" onclick="toggleMenu(this)">内容管理</div>
                <div class="submenu">
                    <a href="#">文章发布</a>
                    <a href="#">评论审核</a>
                </div>
            </li>
        </ul>
    </div>

    <script>
        // 简单的点击切换逻辑
        function toggleMenu(element) {
            // 获取父级 li 元素
            const parent = element.parentElement;
            // 切换 active 类
            parent.classList.toggle('active');
        }
    </script>

</body>
</html>
```