go version好的，下面是分别在 Linux 上安装 Go 和 Docker 的步骤。

### 在 Linux 上安装 Go

1. **下载 Go 的安装包**：
   访问 Go 官方网站下载适用于 Linux 的 Go 语言安装包。假设最新稳定版本为 `1.19.3`，则可以使用以下命令下载：
   ```sh
   wget https://golang.org/dl/go1.19.3.linux-amd64.tar.gz
   ```

2. **解压缩安装包**：
   将下载的安装包解压到 `/usr/local` 目录下：
   ```sh
   sudo tar -C /usr/local -xzf go1.19.3.linux-amd64.tar.gz
   ```

3. **设置环境变量**：
   配置系统的环境变量，以便可以在任何地方运行 Go：
   ```sh
   sudo nano /etc/profile
   ```
   在文件末尾添加以下行：
   ```sh
   export PATH=$PATH:/usr/local/go/bin
   ```
   然后保存文件并退出编辑器。使更改生效：
   ```sh
   source /etc/profile
   ```

4. **验证安装**：
   检查 Go 是否正确安装：
   ```sh
   go version
   ```

### 在 Linux 上安装 Docker

1. **更新系统**：
   首先确保您的系统是最新的：
   ```sh
   sudo apt-get update
   sudo apt-get upgrade
   ```

2. **安装必要的软件包**：
   安装一些必要的软件包，用于处理 HTTPS 传输和 GPG 密钥管理：
   ```sh
   sudo apt-get install apt-transport-https ca-certificates curl gnupg-agent software-properties-common
   ```

3. **添加 Docker 的 GPG 密钥**：
   添加 Docker 的官方 GPG 密钥：
   ```sh
   curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
   ```

4. **添加 Docker 存储库**：
   添加 Docker 的稳定版存储库：
   ```sh
   sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
   ```

5. **更新 apt 包索引**：
   更新 apt 包索引：
   ```sh
   sudo apt-get update
   ```

6. **安装 Docker**：
   安装 Docker 引擎：
   ```sh
   sudo apt-get install docker-ce docker-ce-cli containerd.io
   ```

7. **验证安装**：
   检查 Docker 是否正确安装：
   ```sh
   sudo docker --version
   ```

8. **启动 Docker 服务**：
   启动 Docker 服务并设置开机启动：
   ```sh
   sudo systemctl start docker
   sudo systemctl enable docker
   ```

完成以上步骤后，您应该已经在 Linux 系统上成功安装了 Go 和 Docker。如果有任何问题，请随时告诉我。ls
