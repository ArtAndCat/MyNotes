


```cpp
#include <boost/asio.hpp>
#include <iostream>
#include <string>

int main() {
  try {
    boost::asio::io_context io_context;

    // 1. 创建 acceptor 对象，监听 12345 端口（IPv4 任意地址）
    boost::asio::ip::tcp::acceptor acceptor(
        io_context,
        boost::asio::ip::tcp::endpoint(boost::asio::ip::tcp::v4(), 12345));

    std::cout << "Server is listening on port 12345..." << std::endl;

    // 2. 循环处理客户端连接
    while (true) {
      // 3. 接受一个客户端连接（阻塞）
      boost::asio::ip::tcp::socket socket(io_context);
      acceptor.accept(socket);

      std::cout << "New client connected: "
                << socket.remote_endpoint().address().to_string() << ":"
                << socket.remote_endpoint().port() << std::endl;

      // 4. 与客户端通信：读取数据并回写（echo）
      boost::system::error_code ec;
      for (;;) {
        char data[4096];

        size_t length = socket.read_some(boost::asio::buffer(data), ec);

        if (ec == boost::asio::error::eof) {
          std::cout << "Client disconnected." << std::endl;
          break;  // 连接正常关闭
        } else if (ec) {
          throw boost::system::system_error(ec);  // 其他错误
        }
        char data2[4096];
        snprintf(data2, sizeof(data2), "s: %s ~\n ", data);
        // 将收到的数据原样发回
        boost::asio::write(socket, boost::asio::buffer(data2, strlen(data2)));
      }
    }
  } catch (std::exception& e) {
    std::cerr << "Exception: " << e.what() << std::endl;
    return 1;
  }
  return 0;
}
```


预期:
```txt
Server is listening on port 12345...
```
然后可以新建一个终端连接  比如下面这个命令  127.X.X.X 都是本机地址 
```
telnet 127.0.0.1 12345
```