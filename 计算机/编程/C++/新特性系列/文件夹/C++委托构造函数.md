#C #编程 #新特性 

委托制造函数
```cpp
class Test { // 类
public:
    Test(int n) { 
        标记
    }
    Test() : Test (0) {  // 这里
        标记2
    }
};
```
用作类成员变量初始化的初始化列表

如果调用默认初始化程序会先调用Test(0)然后再次运行标记2
无参构造->有参构造(0) -> 标记 -> 标记2